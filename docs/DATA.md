# Data

Data is not included in this public repo.

To run locally:
1) Create a `data/` folder at the project root.
2) Put your datasets in:
   - data/raw/
   - data/processed/
   - data/global_regions.csv
3) Run:
   python run_path_a.py
